name: Build project and run CI checks
on:
  pull_request:

jobs:
  build-and-check:
    name: Build project and run CI checks
    uses: fingerprintjs/dx-team-toolkit/.github/workflows/build-typescript-project.yml@v1
    with:
      yarnFlags: "--fpcdn-domain=${{secrets.FPCDN_DOMAIN}} --fpjs-domain=${{secrets.FPJS_DOMAIN}}"